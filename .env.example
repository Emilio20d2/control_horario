
# Este es un archivo de ejemplo.
# Copia este contenido a un nuevo archivo llamado .env.local en la raíz de tu proyecto.
# NUNCA compartas tu archivo .env.local ni lo subas a un repositorio público.

# Credenciales de tu proyecto de Firebase
# Puedes encontrar estos valores en la configuración de tu proyecto en la consola de Firebase.
# Configuración del Proyecto -> Cuentas de servicio -> Generar nueva clave privada (esto te dará un JSON con toda la info)

# El ID de tu proyecto de Firebase
FIREBASE_PROJECT_ID="tu-project-id"

# El email de la cuenta de servicio de Firebase
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@tu-project-id.iam.gserviceaccount.com"

# La clave privada de la cuenta de servicio. Copia el contenido completo, incluyendo "-----BEGIN PRIVATE KEY-----" y "-----END PRIVATE KEY-----".
# Asegúrate de reemplazar los saltos de línea literales (\n) por saltos de línea reales en el archivo .env.local si es necesario,
# o mantenlos como \n si tu sistema de despliegue lo requiere (como Vercel).
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...tu-clave-privada...\n-----END PRIVATE KEY-----\n"

# La URL de tu base de datos de Firestore (opcional si ya está en la config del cliente, pero bueno tenerla)
NEXT_PUBLIC_FIREBASE_DATABASE_URL="https://tu-project-id-default-rtdb.europe-west1.firebasedatabase.app"
